From ae86b7ff5e6f3dc37a738908a0e42fcdb3f57ec1 Mon Sep 17 00:00:00 2001
From: gavanderhoorn <g.a.vanderhoorn@tudelft.nl>
Date: Fri, 24 Sep 2021 18:19:57 +0200
Subject: [PATCH 2/7] Special strerror_r handling

---
 src/strerror.c | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/src/strerror.c b/src/strerror.c
index a425010..0dae670 100644
--- a/src/strerror.c
+++ b/src/strerror.c
@@ -27,6 +27,13 @@ rcutils_strerror(char * buffer, size_t buffer_length)
 {
 #if defined(_WIN32)
   strerror_s(buffer, buffer_length, errno);
+#elif defined(YASKAWA_MOTOMAN_MOTOPLUS1)
+  // TODO: Fix. Old compiler strerror_r(..) does not take buffer_length
+  int error_status = strerror_r(errno, buffer);
+  if (error_status != 0) {
+    strncpy(buffer, "Failed to get error", buffer_length);
+    buffer[buffer_length - 1] = '\0';
+  }
 #elif defined(_GNU_SOURCE) && (!defined(ANDROID) || __ANDROID_API__ >= 23) && !defined(__QNXNTO__)
   /* GNU-specific */
   char * msg = strerror_r(errno, buffer, buffer_length);
-- 
2.43.0

